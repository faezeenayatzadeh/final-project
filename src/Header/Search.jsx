const Search = (props) => {
    return (
        <div>
            <img />
            <span>{props.placeholder}</span>
            <input />
            <img />
        </div>
    )
}

export default Search;